# NOTE: You can put Private env vars that you want to keep out of source control 
# by placeing them in a file similar to this named .env

# these are directories in the container intentioned for data and logs
DATA_DIR=/data
LOG_DIR=/logs

# SCRIPT is the name of the script in project/process_scripts that will run when the base docker image is run
SCRIPT=

CIPHERTEXT=
KEYNAME=
KEYRING=
